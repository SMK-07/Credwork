version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: credwork_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: credwork_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
